/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MemberPipe } from './member.pipe';

describe('MemberPipe', () => {
  it('create an instance', () => {
    const pipe = new MemberPipe();
    expect(pipe).toBeTruthy();
  });
});
